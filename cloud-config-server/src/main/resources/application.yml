server:
  port: 9095

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rajesh7020/config-server
          clone-on-start: true